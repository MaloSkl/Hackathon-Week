Démarer un nouveaux projet symfony easyadmin
$ symfony new admin --webapp
$ cd admin/
$ composer require easycorp/easyadmin-bundle

Configurer un proxy
$ symfony proxy:domain:attach 6tm_admin
$ symfony proxy:start
$ symfony server:start

Test your localhost:<PORT>

Create your first Dashboard
$ php bin/console make:admin:dashboard

Test your localhost:<PORT>/admin

Create Entity/Repository
$ php bin/console make:entity

install php with postgres
$ sudo dnf install php-pgsql

metre a jour les info de sa db localhost
$ composer dump-env local
$ php bin/console doctrine:database:create

$ php bin/console cache:clear
$ php bin/console doctrine:migrations:diff
$ php bin/console doctrine:migrations:migrate

installer les assets
$ yarn install
$ yarn encore dev
